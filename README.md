# Django Base Project

Базовый Django-проект с аутентификацией пользователей, расширенными профилями, нефтяными компаниями, скважинами и модулем суточных рапортов по добыче.

---

## Функционал

### 🏢 Компании (OilCompany)
- создание компаний
- редактирование компаний
- удаление компаний
- просмотр списка компаний
- связь с пользователями и скважинами
- фильтрация и сортировка

---

### 👤 Пользователи и профили
- регистрация пользователей
- вход и выход из системы
- автоматическое создание профиля
- CRUD пользователей
- редактирование профиля (включая других пользователей)
- поля профиля:
  - компания
  - отдел
  - телефон
  - bio
- просмотр списка пользователей
- фильтрация по компании
- сортировка
- пагинация
- доступ только для авторизованных пользователей
- управление через Django Admin

---

### 🛢 Скважины (Well)
- создание скважин
- редактирование скважин
- удаление скважин
- просмотр списка скважин
- поля:
  - название
  - компания
  - тип
  - максимальная глубина
  - координаты (latitude, longitude)
- фильтрация по компании
- сортировка
- пагинация
- управление через Django Admin

---

### 📊 Суточные рапорты по добыче (DailyProduction)
- создание рапортов
- редактирование рапортов
- удаление рапортов
- просмотр списка рапортов
- автоматический расчет чистого дебита нефти
- фильтрация:
  - по компании
  - по скважине
  - по диапазону дат
- сортировка
- пагинация
- защита страниц через авторизацию
- управление через Django Admin

---

## Используемые технологии

- Python
- Django
- SQLite3

---

## Структура проекта

```bash
base_project/
│
├── accounts/                 # пользователи и профили
├── productions/              # скважины и рапорты
├── companies/                # компании
├── config/                   # настройки проекта
├── manage.py
├── db.sqlite3
├── README.md
└── .gitignore
```

---

## Установка и запуск проекта (Docker)

Проект полностью настроен для работы в Docker. Для установки и запуска не требуется локальная установка Python или Node.js.

### 1. Клонировать репозиторий
```bash
git clone https://github.com/USERNAME/REPOSITORY_NAME.git
cd REPOSITORY_NAME
```

### 2. Запуск одной командой
В проекте настроен `Makefile`, который автоматически создаст ключи конфигурации, настроит базу данных (MySQL), выполнит миграции базы данных и запустит приложение:

```bash
make up
```

Дождитесь сообщения об успешном запуске (это может занять несколько минут при первой сборке).

### 3. Доступ к сервисам
После запуска проект будет доступен по следующим адресам:
- **Веб-приложение (Nginx):** http://localhost:8086
- **Админка (Django Admin):** http://localhost:8086/admin/
- **Управление БД (Adminer):** http://localhost:8084
- **Почтовый сервер (Mailpit):** http://localhost:8027

### 4. Создание суперпользователя (Админа)
Для входа в панель администратора выполните:
```bash
make superuser
```

### 5. Остановка проекта
```bash
make down
```

---

## Основные страницы

### Пользователи
- `/accounts/register/`
- `/accounts/login/`
- `/accounts/logout/`
- `/accounts/profile/`
- `/accounts/users/`

### Компании
- `/companies/`

### Скважины
- `/productions/wells/`

### Суточные рапорты
- `/productions/`

### Админ-панель
- `/admin/`

---

## Модели проекта

### Profile
Расширяет стандартную модель пользователя Django.

Поля:
- `bio`
- `department`
- `phone_number`
- `oil_company`

---

### OilCompany
Модель нефтяной компании.

Поля:
- `name`
- `region`

Связи:
- имеет пользователей
- имеет скважины

---

### Well
Модель скважины.

Поля:
- `name`
- `oil_company`
- `type`
- `max_drilling_depth`
- `latitude`
- `longitude`

---

### DailyProduction
Модель суточного рапорта.

Поля:
- `well`
- `date`
- `work_time`
- `liquid_debit`
- `water_cut`
- `oil_density`

---

## Бизнес-логика

Формула расчета нефти:

```text
Дебит нефти = Дебит жидкости × (1 - Обводненность / 100) × Плотность нефти
```

---

## Валидация

- все поля обязательны
- `work_time` — от 0 до 24
- `water_cut` — от 0 до 100
- уникальность: `(well, date)`

Все ошибки переведены на русский язык.

---

## CRUD-функционал

Реализован для:

- пользователей
- компаний
- скважин
- суточных рапортов

---

## Интерфейс

Во всех списках реализовано:

- табличное отображение
- пагинация (20 записей)
- сортировка
- фильтрация
- отображение общего количества записей

---

## Админ-панель

Добавлены:

- фильтры (`list_filter`)
- поиск (`search_fields`)
- сортировка (`ordering`)

---


## Автор

Зинетов Алихан  
AITU 🚀

Проект создан как расширенный учебный проект Django с полноценной архитектурой CRUD, связями моделей и бизнес-логикой.
